require 'spec_helper'

describe Introduce do
  it 'has a version number' do
    expect(Introduce::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
